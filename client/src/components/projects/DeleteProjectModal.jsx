export default function DeleteProjectModal({ project, onClose, onConfirm, deleting }) { if (!project) return null; return <div className="fixed inset-0 z-40 grid place-items-center bg-slate-950/75 p-4" role="presentation"><section className="w-full max-w-md rounded-2xl border border-white/10 bg-slate-900 p-6 shadow-2xl" role="dialog" aria-modal="true" aria-labelledby="delete-title"><h2 id="delete-title" className="text-xl font-semibold">Delete Project</h2><p className="mt-3 text-sm text-slate-400">Delete <span className="font-medium text-slate-200">{project.title}</span>? This action cannot be undone.</p><div className="mt-6 flex justify-end gap-3"><button onClick={onClose} disabled={deleting} className="rounded-lg px-4 py-2 text-sm font-medium text-slate-300 hover:bg-white/5">Cancel</button><button onClick={onConfirm} disabled={deleting} className="rounded-lg bg-rose-600 px-4 py-2 text-sm font-medium hover:bg-rose-500 disabled:opacity-50">{deleting ? 'Deleting…' : 'Delete'}</button></div></section></div>; }
