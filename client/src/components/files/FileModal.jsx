import { useState } from 'react';
import { X } from 'lucide-react';
export function NewFileModal({ open, onClose, onCreate }) { const [path, setPath] = useState(''); const [error, setError] = useState(''); const [saving, setSaving] = useState(false); if (!open) return null; const submit = async (event) => { event.preventDefault(); if (!path.trim()) return setError('A file path is required.'); setSaving(true); setError(''); try { await onCreate({ path: path.trim(), content: '' }); setPath(''); onClose(); } catch (requestError) { setError(requestError.response?.data?.error ?? 'Unable to create file.'); } finally { setSaving(false); } }; return <div className="fixed inset-0 z-40 grid place-items-center bg-slate-950/75 p-4"><form onSubmit={submit} className="w-full max-w-md rounded-2xl border border-white/10 bg-slate-900 p-6 shadow-2xl" role="dialog" aria-modal="true" aria-labelledby="new-file-title"><div className="flex items-center justify-between"><h2 id="new-file-title" className="text-lg font-semibold">New file</h2><button type="button" onClick={onClose} aria-label="Close"><X size={18} /></button></div><label className="mt-5 block text-sm">Relative path<input autoFocus value={path} onChange={(event) => setPath(event.target.value)} placeholder="src/components/Hero.jsx" maxLength="255" className="mt-2 w-full rounded-lg border border-white/10 bg-slate-800 px-3 py-2.5 outline-none focus:border-violet-400" /></label>{error && <p className="mt-3 text-sm text-rose-300">{error}</p>}<div className="mt-6 flex justify-end gap-3"><button type="button" onClick={onClose} className="rounded-lg px-4 py-2 text-sm text-slate-300">Cancel</button><button disabled={saving} className="rounded-lg bg-violet-600 px-4 py-2 text-sm font-medium hover:bg-violet-500 disabled:opacity-50">{saving ? 'Creating…' : 'Create file'}</button></div></form></div>; }
export function DeleteFileModal({ file, onClose, onConfirm, deleting }) { if (!file) return null; return <div className="fixed inset-0 z-40 grid place-items-center bg-slate-950/75 p-4"><section className="w-full max-w-md rounded-2xl border border-white/10 bg-slate-900 p-6 shadow-2xl" role="dialog" aria-modal="true"><h2 className="text-lg font-semibold">Delete file</h2><p className="mt-3 text-sm text-slate-400">Delete <span className="font-mono text-slate-200">{file.path}</span>? This action cannot be undone.</p><div className="mt-6 flex justify-end gap-3"><button onClick={onClose} disabled={deleting} className="rounded-lg px-4 py-2 text-sm text-slate-300">Cancel</button><button onClick={onConfirm} disabled={deleting} className="rounded-lg bg-rose-600 px-4 py-2 text-sm font-medium hover:bg-rose-500 disabled:opacity-50">{deleting ? 'Deleting…' : 'Delete'}</button></div></section></div>; }
