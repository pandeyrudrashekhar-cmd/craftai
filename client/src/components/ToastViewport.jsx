import { AnimatePresence, motion } from 'framer-motion';
import { CheckCircle2, X, XCircle } from 'lucide-react';
import { useToastStore } from '../store/toastStore';
export default function ToastViewport() { const { toasts, removeToast } = useToastStore(); return <div className="fixed right-4 top-4 z-50 space-y-3" aria-live="polite"><AnimatePresence>{toasts.map((toast) => <motion.div key={toast.id} initial={{ opacity: 0, y: -12 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, x: 20 }} className="flex max-w-sm items-center gap-3 rounded-xl border border-white/10 bg-slate-800 px-4 py-3 shadow-xl"><>{toast.type === 'error' ? <XCircle size={18} className="text-rose-400" /> : <CheckCircle2 size={18} className="text-emerald-400" />}</><p className="flex-1 text-sm">{toast.message}</p><button aria-label="Dismiss notification" onClick={() => removeToast(toast.id)} className="text-slate-400 hover:text-white"><X size={16} /></button></motion.div>)}</AnimatePresence></div>; }
